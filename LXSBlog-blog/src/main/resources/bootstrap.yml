server:
  port: 7777
spring:
  application:
    name: blog-blog
  main:
    allow-circular-references: true
  cloud:
    nacos:
      discovery:
        server-addr:
      config:
        server-addr:
        file-extension: yaml #指定yaml格式的配置
      username:
      password: