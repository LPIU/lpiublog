server:
  port: 8989
spring:
  application:
    name: admin-blog
  main:
    allow-circular-references: true
  cloud:
    nacos:
      discovery:
        server-addr:
      config:
        server-addr:
        file-extension:
      username:
      password: